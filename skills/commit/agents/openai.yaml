interface:
  display_name: "Commit"
  short_description: "Create and push scoped git commits"
  default_prompt: "Use $commit to inspect local changes, stage intended files, run focused tests, create a structured git commit, sync with main, resolve safe sync conflicts, and push the current branch."
