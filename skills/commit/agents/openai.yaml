interface:
  display_name: "Commit"
  short_description: "Create scoped git commits"
  default_prompt: "Use $commit to inspect local changes, stage intended files, run focused tests, and create a structured git commit."
