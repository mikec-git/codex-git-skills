interface:
  display_name: "PR"
  short_description: "Create or update GitHub PRs"
  default_prompt: "Use $pr to sync with main, resolve safe sync conflicts, push the current branch, and create or update a GitHub pull request with a structured body."
