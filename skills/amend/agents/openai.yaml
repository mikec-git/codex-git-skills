interface:
  display_name: "Amend"
  short_description: "Amend the latest git commit"
  default_prompt: "Use $amend to inspect local changes, stage intended files, run focused tests, amend the latest commit, and push safely when needed."
