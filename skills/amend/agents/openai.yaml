interface:
  display_name: "Amend"
  short_description: "Amend and push the latest git commit"
  default_prompt: "Use $amend to inspect local changes, stage intended files, run focused tests, amend the latest commit, sync with main, resolve safe sync conflicts, and push the rewritten current branch safely."
